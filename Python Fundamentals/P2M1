quote = input("Welcome, FATIMA LOZANO. Enter a 1 sentence quote, non-alpha seperate words: ")
word = ""

for character in quote.lower(): 
    if character.isalpha(): 
        word += character 
    else: 
        if word >= "h":
            print(word.upper())
            word = ""
        else:
            word = ""